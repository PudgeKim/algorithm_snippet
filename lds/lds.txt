Longest Decreasing Sequence

개수에 맞게 배열 만들고
arr = [0]*10
차례대로 배열에 넣는데 처음값은 당연히 0번 인덱스에 넣고
그 이후부터는 배열안에 input값보다 작거나 같은게 있으면 갱신한다

ex)
arr = [389, 207, 155, 0, 0, 0]
input값이 300이면
arr = [389, 300, 155, 0, 0, 0]이 된다
이후에 input값이 68이면
arr = [389, 300, 155, 68, 0, 0]이 된다.

이렇게 최대값을 구하면 됨

